<%= form_for(@memeber_ship) do |f| %>
  <% if @memeber_ship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@memeber_ship.errors.count, "error") %> prohibited this memeber_ship from being saved:</h2>

      <ul>
      <% @memeber_ship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :member_id %><br />
    <%= f.text_field :member_id %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br />
    <%= f.text_field :project_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
